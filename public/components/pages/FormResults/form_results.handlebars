{{#if form}}
    <div class="form-results">
        <div class="form-results__description">
            <h3 class="form-results__description-title">Результаты опроса</h3>
            <h3 class="form-results__description-title">{{form.title}}</h3>
            <p class="form-results__description-text">
            Дата создания: &nbsp;&nbsp;&nbsp;<a>{{form.created_at}}</a><br>
            Количесто прохождений: &nbsp;&nbsp;&nbsp;{{form.number_of_passages}}<br>
            {{#ifEquals form.anonymous true}}
                    Это анонимный опрос. Участники скрыты.
                </p>
            {{/ifEquals}}
            {{#ifEquals form.anonymous false}}
                    Участники:
                </p>
                <div class="form-results_description-text_participants">
                    {{#each ../form.participants}}
                        <p class="form-results_description-text__participants-item">{{first_name}}</p>
                    {{/each}}
                </div>
            {{/ifEquals}}
        </div>
        <div class="check-form__questions-container" id="check-form__questions-container"></div>
        <div class="button-container">
            <button class="secondary-button" type="submit" id="results-export-button">Экспорт данных</button>
        </div>
    </div>
{{/if}}